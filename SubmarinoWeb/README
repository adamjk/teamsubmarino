This directory is the project directory for web application-site. HTML, js, css, php, web application controllers/API(REST), etc... This directory should be able to be built and have a fully functioning web application/site.

Base directory of compilable/interpretable webapp/site code is web-root (old habits die hard)

